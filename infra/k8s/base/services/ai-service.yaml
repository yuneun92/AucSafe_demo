apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: aucsafe
  labels:
    app.kubernetes.io/name: ai-service
    app.kubernetes.io/component: ai
    app.kubernetes.io/part-of: aucsafe
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ai-service
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
