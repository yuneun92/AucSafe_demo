# AucSafe 모바일 개발 계획

## 목차
- [개요](#개요)
- [개발 범위](#개발-범위)
- [Phase 1: 웹앱 모바일 최적화](#phase-1-웹앱-모바일-최적화)
- [Phase 2: React Native 앱 개발](#phase-2-react-native-앱-개발)
- [공통 컴포넌트 전략](#공통-컴포넌트-전략)
- [일정](#일정)

---

## 개요

AucSafe 플랫폼의 모바일 사용자 경험을 개선하기 위해 두 가지 방향으로 개발을 진행합니다:

1. **웹앱 모바일 최적화 (PWA)**: 기존 Next.js 웹앱의 반응형 UI 개선
2. **React Native 앱**: iOS/Android 네이티브 앱 개발

### 현재 상태

#### 웹앱
- 기본적인 모바일 반응형 구현됨
- 모바일 하단 네비게이션 있음
- 개선 필요 사항:
  - 터치 인터랙션 최적화
  - 모바일 전용 UI 패턴 적용
  - PWA 기능 강화

#### React Native
- Expo 기반 프로젝트 설정 완료
- NativeWind (Tailwind CSS) 설정됨
- 실제 앱 코드 없음 (개발 필요)

---

## 개발 범위

### 웹앱 모바일 최적화

| 항목 | 현재 상태 | 목표 |
|------|----------|------|
| 반응형 레이아웃 | 기본 구현 | 완전 최적화 |
| 터치 제스처 | 미구현 | 스와이프, 풀투리프레시 |
| 하단 네비게이션 | 구현됨 | UI 개선 |
| PWA 매니페스트 | 기본 설정 | 완전 구현 |
| 오프라인 지원 | 미구현 | Service Worker |
| 푸시 알림 | 미구현 | Web Push API |

### React Native 앱

| 화면 | 우선순위 | 설명 |
|------|---------|------|
| 홈/랜딩 | P0 | 메인 대시보드 |
| 매물 목록 | P0 | 검색, 필터, 지도 |
| 매물 상세 | P0 | 상세 정보, AI 분석 |
| AI 채팅 | P0 | 챗봇 인터페이스 |
| 관심 목록 | P1 | 즐겨찾기 관리 |
| 등기부 분석 | P1 | 문서 업로드, 분석 결과 |
| 알림 | P1 | 푸시 알림 목록 |
| 프로필 | P2 | 설정, 계정 관리 |
| 인증 | P2 | 로그인, 회원가입 |

---

## Phase 1: 웹앱 모바일 최적화

### 1.1 터치 인터랙션 개선

```typescript
// 적용할 패턴
- 스와이프로 매물 카드 넘기기
- 풀투리프레시
- 바텀시트 (Drawer)
- 햅틱 피드백
```

### 1.2 모바일 전용 컴포넌트

| 컴포넌트 | 설명 |
|---------|------|
| MobilePropertyCard | 모바일 최적화된 매물 카드 |
| MobileBottomSheet | 상세 정보 바텀시트 |
| MobileMapView | 전체화면 지도 |
| MobileFilterDrawer | 필터 드로어 |
| MobileChatInput | 모바일 채팅 입력 |

### 1.3 PWA 강화

```json
// manifest.json 업데이트
{
  "display": "standalone",
  "orientation": "portrait",
  "theme_color": "#0f172a",
  "background_color": "#0f172a",
  "shortcuts": [
    {
      "name": "매물 검색",
      "url": "/search"
    },
    {
      "name": "AI 상담",
      "url": "/chat"
    }
  ]
}
```

### 1.4 Service Worker

```javascript
// 오프라인 캐싱 전략
- 정적 에셋: Cache First
- API 응답: Network First with Cache Fallback
- 이미지: Stale While Revalidate
```

---

## Phase 2: React Native 앱 개발

### 2.1 프로젝트 구조

```
mobile/
├── app/                      # Expo Router 페이지
│   ├── (tabs)/              # 탭 네비게이션
│   │   ├── index.tsx        # 홈
│   │   ├── search.tsx       # 매물 검색
│   │   ├── chat.tsx         # AI 상담
│   │   ├── favorites.tsx    # 관심 목록
│   │   └── profile.tsx      # 프로필
│   ├── property/
│   │   └── [id].tsx         # 매물 상세
│   ├── auth/
│   │   ├── login.tsx
│   │   └── register.tsx
│   └── _layout.tsx
├── components/
│   ├── ui/                  # 공통 UI 컴포넌트
│   ├── property/            # 매물 관련 컴포넌트
│   ├── chat/                # 채팅 컴포넌트
│   └── map/                 # 지도 컴포넌트
├── hooks/                   # 커스텀 훅
├── stores/                  # Zustand 스토어
├── services/                # API 서비스
├── utils/                   # 유틸리티
└── constants/               # 상수
```

### 2.2 기술 스택

| 카테고리 | 라이브러리 |
|---------|-----------|
| 프레임워크 | Expo SDK 51 |
| 라우팅 | Expo Router |
| 스타일링 | NativeWind (Tailwind) |
| 상태관리 | Zustand |
| API | React Query + Axios |
| 지도 | react-native-maps |
| 인증 | expo-secure-store |
| 푸시알림 | expo-notifications |

### 2.3 핵심 화면 설계

#### 홈 화면
```
┌─────────────────────────────┐
│  AucSafe        🔔  👤      │
├─────────────────────────────┤
│  ┌─────────────────────┐    │
│  │  🔍 검색...         │    │
│  └─────────────────────┘    │
│                             │
│  📊 오늘의 추천 매물         │
│  ┌─────┐ ┌─────┐ ┌─────┐   │
│  │     │ │     │ │     │   │
│  │ 매물 │ │ 매물 │ │ 매물 │   │
│  └─────┘ └─────┘ └─────┘   │
│                             │
│  🤖 AI 빠른 상담            │
│  ┌─────────────────────┐    │
│  │ "강남 아파트 추천해줘" │    │
│  └─────────────────────┘    │
│                             │
├─────────────────────────────┤
│  🏠   🔍   💬   ❤️   👤    │
└─────────────────────────────┘
```

#### 매물 검색 화면
```
┌─────────────────────────────┐
│  🔍 검색...        필터 지도 │
├─────────────────────────────┤
│  ┌─────────────────────────┐│
│  │                         ││
│  │       지도 영역          ││
│  │                         ││
│  └─────────────────────────┘│
│  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ │
│  ┌─────────────────────────┐│
│  │ 📸 강남 래미안 1차       ││
│  │ 최저가 3.2억 | AI 92점  ││
│  └─────────────────────────┘│
│  ┌─────────────────────────┐│
│  │ 📸 서초 푸르지오         ││
│  │ 최저가 4.1억 | AI 88점  ││
│  └─────────────────────────┘│
├─────────────────────────────┤
│  🏠   🔍   💬   ❤️   👤    │
└─────────────────────────────┘
```

#### AI 채팅 화면
```
┌─────────────────────────────┐
│  ← AI 상담                  │
├─────────────────────────────┤
│                             │
│     ┌───────────────────┐   │
│     │ 안녕하세요! 어떤   │   │
│     │ 매물을 찾으시나요? │   │
│     └───────────────────┘   │
│                             │
│  ┌───────────────────┐      │
│  │ 강남 아파트 5억   │      │
│  │ 이하로 찾아줘     │      │
│  └───────────────────┘      │
│                             │
│     ┌───────────────────┐   │
│     │ 3개의 매물을 찾았 │   │
│     │ 습니다:          │   │
│     │ 📸 강남 래미안   │   │
│     │ 📸 서초 푸르지오 │   │
│     └───────────────────┘   │
│                             │
├─────────────────────────────┤
│  ┌─────────────────┐  📷 🎤│
│  │ 메시지 입력...   │       │
│  └─────────────────┘        │
└─────────────────────────────┘
```

### 2.4 API 연동

```typescript
// services/api.ts
const API_BASE_URL = process.env.EXPO_PUBLIC_API_URL

export const propertyApi = {
  getList: (params) => axios.get('/api/v1/properties', { params }),
  getDetail: (id) => axios.get(`/api/v1/properties/${id}`),
  search: (query) => axios.get('/api/v1/properties/search', { params: { q: query } }),
}

export const chatApi = {
  sendMessage: (message) => axios.post('/api/v1/chat', { message }),
  getHistory: () => axios.get('/api/v1/chat/history'),
}
```

---

## 공통 컴포넌트 전략

### 디자인 토큰 공유

웹과 React Native에서 동일한 디자인 토큰을 사용합니다:

```typescript
// shared/tokens.ts
export const colors = {
  primary: '#3b82f6',
  secondary: '#64748b',
  success: '#22c55e',
  warning: '#f59e0b',
  danger: '#ef4444',
}

export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
}
```

### 비즈니스 로직 공유

```typescript
// shared/utils/property.ts
export function formatPrice(price: number): string {
  const billion = price / 100000000
  return `${billion.toFixed(1)}억`
}

export function getRiskLevel(score: number): 'safe' | 'warning' | 'danger' {
  if (score >= 80) return 'safe'
  if (score >= 60) return 'warning'
  return 'danger'
}
```

---

## 일정

### Phase 1: 웹앱 모바일 최적화

| 주차 | 작업 내용 |
|------|----------|
| 1주차 | 터치 인터랙션 개선, 모바일 컴포넌트 리팩토링 |
| 2주차 | PWA 강화, Service Worker 구현 |
| 3주차 | 테스트 및 성능 최적화 |

### Phase 2: React Native 앱 개발

| 주차 | 작업 내용 |
|------|----------|
| 1주차 | 프로젝트 구조 설정, 공통 컴포넌트 |
| 2주차 | 홈, 매물 목록 화면 |
| 3주차 | 매물 상세, 지도 화면 |
| 4주차 | AI 채팅 화면 |
| 5주차 | 관심 목록, 알림 화면 |
| 6주차 | 인증, 프로필 화면 |
| 7주차 | 테스트 및 버그 수정 |
| 8주차 | 앱스토어 배포 준비 |

---

## 배포

### 웹앱 (PWA)
- GitHub Pages: https://yuneun92.github.io/AucSafe_demo
- 모바일 브라우저에서 "홈 화면에 추가"로 앱처럼 사용 가능

### React Native 앱
- iOS: TestFlight → App Store
- Android: Internal Testing → Google Play Store
- Expo EAS Build 사용

```bash
# 개발 빌드
eas build --profile development --platform all

# 프로덕션 빌드
eas build --profile production --platform all

# 스토어 제출
eas submit --platform all
```

---

## 참고 자료

- [Expo Documentation](https://docs.expo.dev/)
- [NativeWind Documentation](https://www.nativewind.dev/)
- [React Native Maps](https://github.com/react-native-maps/react-native-maps)
- [Next.js PWA](https://github.com/shadowwalker/next-pwa)
